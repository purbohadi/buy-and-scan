# Copy to .dev.vars (not committed) for local Wrangler

# Google OAuth (Web client) — add scopes in Cloud Console: Drive (per-file), Sheets
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Random string (32+ chars): signs session cookies and encrypts stored Google refresh tokens
AUTH_SESSION_SECRET=
